export type Product = {
    id: string;
    name: string;
    price: number;
    description: string;
    stock: string;
    slug: string;
    categoryId?: string;
    createdAt: string;
    updatedAt: string;
};
